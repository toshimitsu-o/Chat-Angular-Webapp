export class Channel {
    id: string;
    name: string;
    gid: string;

    constructor(id: string, name: string, gid: string) {
        this.id = id;
        this.name = name;
        this.gid = gid;
    }
}
