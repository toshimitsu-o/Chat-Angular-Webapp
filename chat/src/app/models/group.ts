export class Group {
    id: string;
    name: string;
    img: string;

    constructor(id: string, name: string, img: string) {
        this.id = id;
        this.name = name;
        this.img = img;
    }
}
